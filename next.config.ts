import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['files.binc.pk','www.gravatar.com','placehold.co','binc-host.s3.us-east-005.backblazeb2.com'],
  },
  
};

export default nextConfig;
